<?xml version="1.0"?>
<launch>
    <!-- get default parameters from yaml -->
	<rosparam file="$(find uav_control)/config/flight.yaml" />

    <!-- start the main node -->
    <node name="test_separate_modules" pkg="uav_control" type="test_separate_modules" output="screen">
    </node>
</launch>
